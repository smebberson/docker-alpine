#!/usr/bin/with-contenv sh

# Get the consul IPs
JOIN=`container-ip consul | awk '{ printf " -retry-join " $0}'`

# If we didn't find an IP, exit 1.
if test -z "$JOIN"; then
    exit 1;
fi

# We found one. Exit 0!
echo "$JOIN"
exit 0
